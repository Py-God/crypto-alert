[project]
name = "crypto-alert"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "alembic>=1.17.2",
    "fastapi[standard]>=0.128.0",
    "uvicorn>=0.40.0",
]
